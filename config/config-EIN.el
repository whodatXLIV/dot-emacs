;;; package --- EIN configuration

;;; Commentary:

;;; Code:
(require-package 'ein)
(require 'ein)
(require 'ein-loaddefs)
(require 'ein-notebook)
(require 'ein-subpackages)

;; (global-set-key (kbd "") 'ein:jupyter-server-start)

(provide 'config-ein)
;;; config-EIN.el ends here
