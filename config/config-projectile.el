;;; package --- projectile configuration

;;; Commentary:

;;; Code:

(require-package 'projectile)
(projectile-mode t)
;; (after! projectile  
;;   (add-to-list 'projectile-globally-ignored-directories "*~/.emacs.d"))
(provide 'config-projectile)
;;; config-projectile.el ends here
